<form action="/cart" method="POST">
    <button type="submit" class="btn" <%= product.stock <= 0 ? 'disabled' : '' %>><%= product.stock <= 0 ? 'Out of Stock' : 'Add to Cart' %></button>
    <input type="hidden" name="productId" value="<%= product.id %>" />
</form>
